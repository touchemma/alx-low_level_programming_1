#include "main.h"

/**
 * _strlen - function that return the length of a string
 *
 * @s: this is input string
 *
 * Return: length of string
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0' ; i++)
	return (i);
}
